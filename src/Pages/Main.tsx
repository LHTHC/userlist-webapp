import { FC } from 'react';

import Table from '../components/UsersTable/Table';

const Main: FC = () => {
  return (
    <div className="main">
      <Table />
    </div>
  );
};

export default Main;
